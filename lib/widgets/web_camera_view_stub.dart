import 'package:flutter/material.dart';

Widget buildWebCameraView(Function(double angle, String status) onUpdate) {
  return const Center(child: Text('Web camera view not supported on this platform.'));
}
